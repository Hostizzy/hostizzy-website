export const metadata = {
  title: 'Login - Hostizzy Admin',
  description: 'Secure access to Hostizzy management console',
};

export default function LoginLayout({ children }) {
  return children;
}
